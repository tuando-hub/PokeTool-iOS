# Interactive 3DS

3DS is never bypassed. A challenge is a manual user-action state; the visible browser remains owned by the task until the user completes or cancels it, subject to an overall timeout. Challenge credentials and issuer URLs are excluded from logs. Final order submission remains explicit-only.
