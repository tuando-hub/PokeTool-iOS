"use strict";
class ProviderError extends Error { constructor(code,message){super(message);this.code=code;} }
class MockPhoneOtpProvider { constructor(options={}){this.options=options;this.orders=new Map();} async authenticate(){if(this.options.failure)throw new ProviderError("PROVIDER_UNAVAILABLE","Mock provider failure");} async orderNumber(){if(this.options.unavailable)throw new ProviderError("NUMBER_UNAVAILABLE","No number available");const id="mock-"+Math.random().toString(36).slice(2);this.orders.set(id,{phone:this.options.phone||"819000000000",otp:this.options.otp||null,expiresAt:Date.now()+60000});return {providerOrderId:id};} async getOrder(id){const o=this.orders.get(id);if(!o)throw new ProviderError("ORDER_NOT_FOUND","Provider order not found");if(this.options.expired)throw new ProviderError("ORDER_EXPIRED","Provider order expired");return {status:"NUMBER_RECEIVED",phone:o.phone};} async getOtp(id){const o=this.orders.get(id);if(!o)throw new ProviderError("ORDER_NOT_FOUND","Provider order not found");if(this.options.timeout)return {status:"WAITING_FOR_SMS",otp:null};return {status:o.otp?"OTP_RECEIVED":"WAITING_FOR_SMS",otp:o.otp};} async cancel(id){this.orders.delete(id);} async release(id){this.orders.delete(id);} }
class UnconfiguredPhoneOtpProvider { async authenticate(){throw new ProviderError("PROVIDER_UNAVAILABLE","Phone provider is not configured");} async orderNumber(){throw new ProviderError("PROVIDER_UNAVAILABLE","Phone provider is not configured");} async getOrder(){throw new ProviderError("PROVIDER_UNAVAILABLE","Phone provider is not configured");} async getOtp(){throw new ProviderError("PROVIDER_UNAVAILABLE","Phone provider is not configured");} async cancel(){} async release(){} }
function createProvider(env=process.env,options={}){return (env.PHONE_OTP_PROVIDER||"unconfigured")==="mock"?new MockPhoneOtpProvider(options):new UnconfiguredPhoneOtpProvider();} module.exports={ProviderError,MockPhoneOtpProvider,UnconfiguredPhoneOtpProvider,createProvider};
